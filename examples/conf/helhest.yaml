defaults:
  - simulation: helhest
  - rendering: gl
  - execution: cuda_graph
  - logging: disabled
  - engine: axion 
  - control: helhest/position
  - _self_

# Use the recommended hybrid approach for project names
project_name: ${hydra:job.name}

friction_coeff: 0.5
