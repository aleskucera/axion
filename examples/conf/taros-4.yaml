defaults:
  - simulation: taros-4
  - rendering: gl
  - execution: cuda_graph
  - logging: disabled
  - engine: axion 
  - control: taros-4/velocity
  - _self_

# Use the recommended hybrid approach for project names
project_name: ${hydra:job.name}

friction_coeff: 0.6
