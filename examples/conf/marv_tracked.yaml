defaults:
  - simulation: marv_tracked
  - rendering: gl
  - execution: cuda_graph
  - logging: disabled
  - engine: axion 
  - control: helhest/velocity
  - _self_

# Use the recommended hybrid approach for project names
project_name: ${hydra:job.name}

engine:
  contact_compliance: 1e-9
  max_contacts_per_world: 1024
