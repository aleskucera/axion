defaults:
  - simulation: helhest_featherstone_diff
  - rendering: gl
  - execution: cuda_graph
  - logging: disabled
  - engine: featherstone
  - control: helhest/velocity_featherstone
  - _self_

project_name: ${hydra:job.name}
